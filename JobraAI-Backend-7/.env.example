# Copy to `.env` and fill in locally. Do not commit real keys — GitHub blocks pushes that contain API keys.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-3.5-turbo
MIN_LLM_RESPONSE_SECONDS=10
MAX_LLM_RESPONSE_SECONDS=15
